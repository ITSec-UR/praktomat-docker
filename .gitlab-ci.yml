stages:
  - deploy

# Define the job to deploy, which will require a TAG as input
deploy_job:
  stage: deploy
  script:
    - echo "Deploying with TAG: $TAG_VARIABLE"
  when: manual
  only:
    - main
