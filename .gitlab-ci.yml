stages:
  - deploy

# Define the job to deploy, which will require a TAG as input
deploy_job:
  stage: deploy
  when: manual
  only:
    - main
  variables:
    TAG_VARIABLE: "default_tag"  # Default value (can be overwritten when triggered manually)
